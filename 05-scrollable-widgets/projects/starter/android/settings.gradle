pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven {
            url = uri("${System.env.FLUTTER_STORAGE_BASE_URL ?: "https://storage.googleapis.com/download.flutter.io"}")
        }
    }
}

include ':app'
